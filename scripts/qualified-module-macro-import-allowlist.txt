# Bare generated-family imports that are intentionally retained.
#
# stdlib/hashmap.tl, stdlib/option.tl, and stdlib/set.tl call macros declared
# in their current module. compiler_typecheck_smoke.tl embeds focused local
# macro fixtures. The safety fixture is negative coverage for rejecting an
# imported self-named macro without its provider qualifier.
stdlib/hashmap.tl:(cfg test (import (hashmap HashNestedKey i64) as hashmap_nested_i64))
stdlib/hashmap.tl:(cfg test (import (hashmap i64 HashState) as hashmap_i64_state))
stdlib/hashmap.tl:(cfg test (import (hashmap i64 i64) as hashmap_i64_i64))
stdlib/hashmap.tl:(cfg test (import (hashmap i64 String) as hashmap_i64_string))
stdlib/hashmap.tl:(cfg test (import (hashmap String HashPayload) as hashmap_string_payload))
stdlib/hashmap.tl:(cfg test (import (hashmap String i64) as hashmap_string_i64))
stdlib/hashmap.tl:(cfg test (import (hashmap String String) as hashmap_string_string))
stdlib/option.tl:(cfg test (import (option i64) as option_i64_again))
stdlib/option.tl:(cfg test (import (option i64) as option_i64))
stdlib/option.tl:(cfg test (import (option OptApiPoint) as option_point))
stdlib/option.tl:(cfg test (import (option OptApiTag) as option_tag))
stdlib/option.tl:(cfg test (import (option String) as option_string))
stdlib/set.tl:(cfg test (import (set i64) as i64set_again))
stdlib/set.tl:(cfg test (import (set i64) as i64set))
stdlib/set.tl:(cfg test (import (set String) as strset))
src/tests/compiler_typecheck_smoke.tl:      (import (vector ,T) as vec)
src/tests/compiler_typecheck_smoke.tl:    (import (vector i64) as v)
src/tests/compiler_typecheck_smoke.tl:(import (vector i64) as vec)
src/tests/compiler_typecheck_smoke.tl:(import (vector Pair) as pvec)
tests/safety/imported_module_macro_bare_name_reject.tl:(import (vector i64) as vec)
