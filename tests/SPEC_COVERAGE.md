# SPEC Coverage Matrix

This matrix tracks the implemented and specified behavior in `SPEC.md`
sections 2 through 11. It is intentionally row-oriented rather than prose-only
so future SPEC edits can update the behavioral row, coverage owner, and
follow-up link in the same change.

## Maintenance

When changing `SPEC.md` sections 2 through 11:

- Add or update a row here in the same PR as the SPEC change.
- Prefer an existing TypeLisp-owned gate before adding a new fixture:
  integration manifests, safety corpus, inline tests, doctests, selfhost smoke
  tests, stdlib tests, public-tool specs, or backend diagnostics.
- Use `GAP` only after reconciling those gates. Link an open issue for every
  independent uncovered behavior.
- For unsupported section 8 behavior, add or link negative/diagnostic coverage
  work instead of positive runtime coverage.
- Re-query the tracker whenever a row is updated. Follow-up cells must link
  only open issues; when an issue closes, re-verify the shipped code and tests,
  then mark the row covered or replace the link with the focused open remainder.

Status values:

- `Covered`: existing CI-owned coverage exercises the behavior.
- `Negative`: existing CI-owned coverage asserts rejection or a runtime trap.
- `GAP`: implemented behavior needs additional coverage.
- `Feature pending`: SPEC-defined behavior is not implemented yet; coverage is
  owned by the feature issue before positive tests can land.
- `Partial`: some implemented behavior is covered, but the row has remaining
  coverage work.

Primary coverage owners referenced below:

- `integration`: `tests/integration/*.tl` through
  `tests/integration/native-linux.manifest`,
  `tests/integration/native-windows.manifest`, and
  `scripts/verify-integration.sh`.
- `safety`: `tests/safety/manifest.txt` and
  `scripts/verify-safety-corpus.sh`.
- `stdlib`: `stdlib/tests/*.tl` through stdlib verification.
- `inline`: inline `(test ...)` discovery through
  `scripts/verify-inline-tests.sh`.
- `doctest`: `scripts/verify-doc-tests.sh` and doctest smoke modules.
- `public-tools`: JSON/spec fixtures and shell assertions under
  `tests/public-tools/` and `scripts/verify-public-tools.sh`.
- `selfhost-smoke`: selfhost smoke drivers in the integration manifests and
  `src/compile_manifest.txt`.

## Section 2: Lexical Structure

| ID | SPEC section | Normative behavior | Implementation status | Coverage owner/test(s) | Gap status | Follow-up |
| --- | --- | --- | --- | --- | --- | --- |
| S2-001 | 2.1 | Reader/lexer produces the fourteen `Token` kinds: delimiters, `Int`/`Float`/`Str`/`Char` literals (including decimal exponent forms with exact spelling), `Sym` for every keyword, identifier, boolean, `unit`, operator and dotted path, quote/quasiquote punctuation, and `End`. | Implemented | `tests/integration/lexer.tl`, `tests/integration/parser.tl`, `src/lex.tl`, `src/read.tl`, `tests/inline/lexer_rust_coverage.tl`, `tests/inline/float_exponent_frontend.tl`, `src/tests/lex_span_smoke.tl`, `tests/integration/float_exponent_literals.tl`, `tests/format_golden/*` | Covered | - |
| S2-002 | 2.2 | Semicolon comments are skipped; public doc comment forms are only `;#` and `;:` with contiguous item attachment rules. | Implemented | `src/tests/doc_extract_smoke.tl`, `tools/doc-site/doc_md_smoke.tl`, `tools/doc-site/doc_site_smoke.tl`, doctest verification | Covered | - |
| S2-003 | 2.2 | `typelisp doc --test` extracts supported Markdown fences, handles `expect-error`, runnable doctest metadata, malformed fences, and package/multi-input reporting. | Implemented | `src/tests/doc_test_smoke.tl`, `scripts/verify-doc-tests.sh`, `scripts/verify-public-tools.sh` SPEC metadata scanner | Covered | - |
| S2-004 | 2.3 | String escapes for newline, tab, carriage return, backslash, and quote are decoded; unknown escapes remain literal today. | Implemented | `tests/integration/string_*`, `tests/integration/lexer.tl`, doctests and format golden fixtures | Covered | - |
| S2-005 | 2.4 | Integer literals default to `i32`, adopt expected integer types with range checks, and finite-only float literals (including exponent-only/fraction+exponent grammar) default to `f64` with contextual `f32` support. Non-finite decimal overflow and NaN/Infinity spellings are rejected deterministically. | Implemented | `src/tests/compiler_typecheck_smoke.tl`, `src/compiler_float.tl`, `tests/inline/float_exponent_frontend.tl`, `tests/integration/arithmetic.tl`, `tests/integration/f32_scalar.tl`, `tests/integration/float_exponent_literals.tl`, `tests/integration/overflow_casts.tl`, `tests/integration/integer_literal_u64_full_range.tl`, `tests/safety/integer_wrap_cast_defined.tl`, `tests/safety/integer_literal_*_reject.tl`, `tests/safety/float_literal_*`, `tests/safety/float_exponent_*` | Covered | - |

## Section 3: Type System

| ID | SPEC section | Normative behavior | Implementation status | Coverage owner/test(s) | Gap status | Follow-up |
| --- | --- | --- | --- | --- | --- | --- |
| S3-001 | 3.1 | Primitive type set, sizes, scalar value behavior, `bool`, `char`, and `unit`. | Implemented | `tests/integration/arithmetic.tl`, `tests/integration/f32_scalar.tl`, `tests/integration/print_char.tl`, `tests/integration/unit_functions.tl`, `tests/integration/unit_main.tl` | Covered | - |
| S3-002 | 3.2 | Tuples, fixed-size public `(Array T N)`, rejection of unsized public `(Array T)`, compiler-private dynamic buffers, and owned `String` source values; private buffers reject invalid lengths, initialize live elements, and support dynamic literal syntax. | Implemented | `src/compiler_parse_core.tl` public-removal/private-buffer parser tests, `tests/integration/tuple_values.tl`, `tests/integration/fixed_array.tl`, `tests/integration/make_array_*.tl`, `tests/integration/dynamic_array_literal.tl`, `tests/integration/string_*`, `tests/integration/array_push_aggregate.tl` | Covered | - |
| S3-003 | 3.2, 3.11 | Bare `str` and `bytes` are not by-value types; borrowed text uses `(& r str)`. | Implemented for `str` and borrowed `bytes` referents | `stdlib/string.tl` inline tests, `stdlib/tests/byte_buf_api.tl`, `stdlib/string_caller_result.tl` inline tests, `src/tests/compiler_typecheck_smoke.tl` borrowed `bytes` positive and bare-value rejection tests, `tests/safety/runtime_type_literal_reject.tl` | Covered | - |
| S3-004 | 3.3 | Function pointer types, direct calls, indirect calls, non-capturing lambdas, and capturing closure descriptor values. | Implemented | `tests/integration/function_pointer_values.tl`, `tests/integration/lambda_lift.tl`, `tests/integration/lambda_capture_*.tl`, `tests/integration/tail_indirect_closure.tl` | Covered | - |
| S3-005 | 3.4, 5.20 | Raw pointer type syntax is safe to mention/copy/pass; dereference/write/offset/cast/address conversion require `unsafe`. | Implemented | `tests/safety/raw_pointer_values_defined.tl`, `tests/safety/safe_raw_pointer_read_reject.tl`, `tests/safety/manual_arena_reset_unsafe_reject.tl`, `stdlib/ffi.tl` inline tests | Covered | - |
| S3-006 | 3.4.1 | `(Box T)`, `box`, `deref`, and dereferenced-place assignment provide safe arena-owned/reference indirection and ownership-aware boxed-storage access; legacy Box projection spellings remain transitional aliases. | Implemented | `tests/integration/unified_place_syntax.tl`, `tests/integration/composed_place_projection.tl`, `tests/integration/box_take_set.tl`, `tests/integration/tree.tl`, `src/compiler_typecheck.tl` box and move-checker fixtures, `tests/safety/box_*_reject.tl`, `tests/safety/deref_*_reject.tl` | Covered | - |
| S3-007 | 3.5.1 | Enums have 0-based tags, nullary and payload variants, global constructor/pattern names, exhaustive match, and heap-allocated returns. | Implemented | `tests/integration/enum_match.tl`, `tests/integration/enum_string_payload.tl`, `tests/integration/nullary_variant_call.tl`, `tests/integration/string_match.tl`, `tests/safety/invalid_struct_field_reject.tl` for aggregate diagnostics | Covered | - |
| S3-008 | 3.5.2 | Structs have call-like constructors, arbitrarily composed dotted field access, field-place mutation, heap-allocated returns, and no struct globals. | Implemented | `tests/integration/struct_field_set.tl`, `tests/integration/composed_place_projection.tl`, `tests/integration/register_pair_struct.tl`, `tests/safety/struct_field_set_*_reject.tl`, `tests/integration/aggregate_globals.tl` | Covered | - |
| S3-009 | 3.5.3 | Default inline layout metadata and `(:repr c)` compatibility preserve declaration-order field offsets and enum tagged-union layout. | Implemented in metadata/layout paths; C ABI lowering separately validated | `tests/integration/repr_c_f32_fields.tl`, `tests/integration/nested_repr_c_aggregate.tl`, `tests/integration/array_stride_24byte.tl`, `tests/integration/ptr_offset_inline_aggregate_stride.tl`, C ABI fixtures | Covered | - |
| S3-010 | 3.6, 3.7 | No type aliases, source-level generics, traits, interfaces, generic functions, or `impl`; use monomorphic or generated declarations. | Implemented as current language policy | `tests/integration/generated_option_result_families.tl`, `src/compiler_parse_core.tl` generic-policy diagnostics, `src/compiler_typecheck.tl` type-valued comptime diagnostics | Covered | - |
| S3-011 | 3.7.1 | Declaration-emitting macro identity, duplicate policy, generated-origin diagnostics, and parse-time rejection of removed `comptime-decl`/`comptime-decls` syntax. | Implemented | `tests/integration/generated_option_result_families.tl`, `src/tests/compiler_parse_smoke.tl` removed-syntax diagnostics, `src/compiler_specialize.tl` generated identity/rebuild tests, `src/compiler_typecheck.tl` generated identity/duplicate tests, `src/tests/compiler_typecheck_smoke.tl` declaration macro generated identity tests, `src/tests/compiler_ctfe_smoke.tl`, `src/tests/compiler_typecheck_reflection_smoke.tl` | Covered | - |
| S3-012 | 3.7.1, 3.7.1.1 | Typed expression macro signatures, expression/clause captures, quote/quasiquote, expansion checks, comptime purity restrictions, and finite-only scalar CTFE float constants/results. | Implemented for the current macro/prelude and stdlib-owned helper surface | `tests/integration/implicit_core_prelude.tl`, `tests/integration/cond.tl`, `tests/integration/macro_forward_order.tl`, `stdlib/sort.tl`, `stdlib/tests/comptime_api.tl`, `stdlib/tests/core_macros_*.tl`, `tests/inline/vector_macro_i64.tl`, `src/compiler_lower.tl` macro `ExprClause` positive/error tests, `src/compiler_typecheck.tl` comptime purity tests, `src/tests/compiler_ctfe_smoke.tl`, `src/tests/compiler_optimize_smoke.tl`, `tests/safety/float_literal_*` | Covered | - |
| S3-013 | 3.7.1.2, 3.7.2 | Stdlib-owned `Expr`/`TypeInfo` shapes and hygienic macro expansion rules. | Dense stdlib-owned syntax/reflection values use the specified short qualified enum variants, and full hygiene is implemented | `tests/bootstrap_comptime_short_variants_probe.tl`, `tests/inline/macro_generated_match_patterns.tl`, `tests/inline/comptime_generated_mut_borrow_macro.tl`, and `tests/inline/comptime_mut_call_builders.tl` cover generated syntax construction; `src/compiler_typecheck.tl` and `src/tests/compiler_typecheck_smoke.tl` cover the well-known ABI diagnostic plus definition/use-site, imported, nested, and splice hygiene; generated-syntax safety fixtures cover diagnostics | Covered | - |
| S3-014 | 3.8 | Numeric casts implement the scalar matrix; non-numeric casts are statically rejected; no implicit conversions. | Implemented | `src/tests/compiler_ctfe_smoke.tl`, `src/tests/compiler_optimize_smoke.tl`, `tests/integration/overflow_casts.tl`, `tests/integration/u64_float_casts.tl`, `tests/safety/integer_wrap_cast_defined.tl`, `tests/safety/unsupported_cast_reject.tl` | Covered | - |
| S3-015 | 3.9 | Region-tagged types from `with-arena` do not change ABI and cannot escape region scope or v1 function boundaries. | Implemented for current region-checker surface | `tests/integration/with_arena_*.tl`, `tests/integration/arena_escape_stress.tl`, `tests/safety/region_escape_reject.tl`, `tests/safety/reference_region_defined.tl` | Covered | - |
| S3-016 | 3.10, 3.10.1 | Reference type syntax, explicit borrows, immutable auto-borrowing and `&mut T` to `&T` typed-call reborrowing, two-phase mutable call borrows, local/parameter/global place-root exclusivity, straight-line, path-sensitive branch, and bounded scalar-loop exit/backedge last-use shortening, lexical lifetime names, stored/returned lifetime-parameterized aggregates, shared/lending/consuming generated-vector iterators, scalar `for` ownership-mode selection for nominal iterables, fixed arrays, and borrowed native Slice values, and escape diagnostics. | Implemented | `tests/integration/auto_borrow_call.tl`, `tests/integration/mut_to_immut_reborrow_call.tl`, `tests/integration/two_phase_mutable_call_borrow.tl`, `tests/integration/lifetime_*.tl`, `tests/integration/ref_*.tl`, `tests/integration/vector_consuming_iterator.tl`, `tests/integration/for_macro.tl`, `tests/integration/spmd_scalar_for_native.tl`, `tests/safety/global_*borrow*`, `tests/safety/auto_borrow_*`, `tests/safety/vector_into_iterator_*`, `tests/safety/for_owned_source_reuse_reject.tl`, `tests/safety/for_native_mut_item_capture_reject.tl`, `tests/safety/for_cleanup_owned_nonfinal_zip_reject.tl`, `tests/safety/mut_to_immut_reborrow_return_live_reject.tl`, `tests/safety/borrow_*_before_last_use_reject.tl`, `tests/safety/borrow_*_after_last_use_ok.tl`, `tests/safety/borrow_while_stored_loop_carried_reject.tl`, `tests/safety/loop_mut_borrow_exit_paths_ok.tl`, `tests/safety/mutable_reference_*`, `tests/safety/two_phase_mutable_call_*`, `stdlib/tests/vector_native_slice_*.tl`, `src/tests/compiler_typecheck_smoke.tl` reference escape/aggregate diagnostics | Covered | - |
| S3-017 | 3.10.4 | Immutable and mutable-reference captures are allowed only for proven non-escaping closures; all-Copy closure aliases retain reference provenance; checker-known ownership and call-capability facts fail closed when erased. | Implemented | `tests/integration/lambda_capture_lifetime_aggregate.tl`, `tests/integration/lambda_capture_capabilities.tl`, `tests/integration/lambda_capture_copy.tl`, `tests/integration/register_group_borrowed_match.tl`, `tests/safety/borrow_lambda_capture_final_use_ok.tl`, `tests/safety/lambda_mut_capture_*`, `tests/safety/lambda_fnonce_*`, `tests/safety/lambda_noncopy_capture_*`, `src/tests/compiler_typecheck_smoke.tl` closure fact tests | Covered | - |
| S3-018 | 3.11 | Owned `String` and borrowed `str` source model, auto-borrowed text APIs, caller-result compatibility, and arena escape rules. | Implemented for borrowed `str`; compatibility builtins remain | `stdlib/string.tl` inline tests, `stdlib/string_caller_result.tl` inline tests, `stdlib/io_caller_result.tl` inline tests, `tests/integration/stdlib_string.tl`, `tests/integration/string_*` | Covered | - |
| S3-019 | 3.11 | `ByteBuf`, `(& r bytes)`, and `(&mut r bytes)` binary storage contract and conversions; allocated binary I/O reads return `ByteBuf` and non-consuming writes borrow `bytes`. | Implemented for the specified owned buffer, borrowed view, mutation, sub-slice, copy-boundary, FFI surface, and whole-file/handle/stdio I/O adoption | `stdlib/tests/byte_buf_api.tl`, `stdlib/io.tl` inline tests, `stdlib/ffi.tl` inline tests, `tests/integration/stdlib_io.tl` embedded-NUL/invalid-UTF-8 round trip, `src/tests/compiler_typecheck_smoke.tl` borrowed `bytes` positive and bare-value rejection tests, and `tests/safety/byte_buf_*` borrow/trap fixtures | Covered | - |
| S3-020 | 3.3, 4.7.2 | `(:consume)` opts runtime non-reference parameters into broad move-only call consumption, is part of static callable identity but not ABI, preserves the effect across direct/qualified/generated/lambda/function-value calls, and leaves copyable and unmarked compatibility behavior unchanged. | Implemented | `tests/integration/consume_parameters.tl`, `tests/safety/consume_parameter_*` | Covered | - |
| S3-021 | 3.7.1.3 | Fixed macro `type` operands may declare one shallow `(:kind ...)` constraint; declarations validate the stable kind vocabulary, concrete calls validate before transformer CTFE/cache reuse, diagnostics identify the macro operand and concrete type, and constraints do not alter generated identity keys or introduce generics/traits. | Implemented, including definition-time abstract body checking and branch refinement | `compiler_parse_smoke.tl` covers successful constraint storage plus empty, duplicate, unknown, misplaced, variadic, repeated, and malformed constraints; `compiler_typecheck_smoke.tl` covers satisfied/rejected expression and generated-module operands, abstract reflection-body checks, branch refinement, exact diagnostics, duplicate generated-module identity, and unconstrained compatibility | Covered | - |
| S3-022 | 3.2, 3.10 | Borrowed unsized `Slice T` referents are legal only behind shared/mutable references; fixed-array whole borrows unsize explicitly at typed calls, checked `slice-view`/`slice-mut-view` subviews preserve provenance and NLL ownership, compiler-trusted `slice-split-at`/`slice-split-at-mut` creates composable range-disjoint sibling views while retaining the owner borrow, and Slice element operations are allocation-free with overflow-safe range and ZST rules. | Implemented | `tests/integration/slice_whole_array.tl`, `tests/integration/slice_views.tl`, `tests/integration/slice_split_at.tl`, `tests/integration/slice_generated_decl.tl`, `tests/golden/slice_text_ir.tl`, `src/compiler_lower_tests.tl` and `src/compiler_backend_tests.tl` emitted-source Slice ABI checks, `stdlib/tests/vector_native_slice_split_grow_live.tl`, and the `tests/safety/slice_*` corpus | Covered | - |
| S3-023 | 3.10.3 | Proven long-to-short lifetime coercion is structural across references, functions, immutable containers, named aggregates, closures, branches, calls, assignments, returns, and native Slice; `&mut` referents, `MutPtr`, mutable dynamic arrays, and mixed-polarity nominal parameters remain invariant, while equality, hashing, mangling, layout, and ABI stay exact. | Implemented | `tests/integration/lifetime_shortening_variance.tl`, `tests/safety/lifetime_shortening_*_reject.tl`, `tests/safety/auto_borrow_shared_lifetime_reject.tl`, `src/compiler_typecheck.tl` lifetime call and match-join tests, `src/tests/compiler_typecheck_smoke.tl` independent-signature-lifetime diagnostic, bootstrap fixpoint and deterministic generated-code gates | Covered | - |

## Section 4: Top-Level Forms

| ID | SPEC section | Normative behavior | Implementation status | Coverage owner/test(s) | Gap status | Follow-up |
| --- | --- | --- | --- | --- | --- | --- |
| S4-001 | 4.1 | Global `define` accepts typed or inferred scalar, string, and aggregate initializers, using generated runtime initializer functions when needed. | Implemented | `tests/integration/global_initializer.tl`, `tests/integration/aggregate_globals.tl`, `src/compiler_lower.tl` global initializer shape tests, `src/compiler_backend_tests.tl` global data/runtime-global shape tests | Covered | - |
| S4-002 | 4.2 | Function `define` requires typed parameters, defaults return to `unit`, supports recursion, entry `main`, synthesized main, and rejects varargs outside externs. | Implemented | `tests/integration/functions.tl`, `tests/integration/factorial.tl`, `tests/integration/unit_functions.tl`, `tests/integration/unit_main.tl`, parser diagnostics | Covered | - |
| S4-003 | 4.3 | Extern forms, exact symbol/link metadata, function pointers, varargs markers, link input dedup, ABI-supported signatures, and rejection of borrowed Slice references in C externs with explicit pointer-plus-length interop guidance. | Implemented | `src/compiler_parse_core.tl` extern metadata/varargs tests, `src/compiler_typecheck.tl` extern ABI tests, `src/compiler_backend_tests.tl` extern metadata/function-pointer/varargs tests, `tests/safety/slice_extern_ref_reject.tl`, `tests/integration/c_abi_slice_ptr_len.tl`, C ABI fixtures, `tests/safety/foreign_abi_metadata_reject.tl`, `stdlib/ffi.tl` inline tests, public-tool build/link coverage | Covered | - |
| S4-004 | 4.3.1 | `(unsafe declaration)` gates safe calls/references to unsafe functions and externs. | Implemented | `src/compiler_parse_core.tl` unsafe declaration tests, `src/compiler_typecheck.tl` unsafe declaration tests, `tests/safety/safe_raw_pointer_read_reject.tl`, `tests/safety/foreign_abi_metadata_reject.tl`, raw-pointer fixtures | Covered | - |
| S4-005 | 4.4 | String-path imports are rejected; filesystem paths remain available to include forms. | Implemented | `src/compiler_parse_core.tl` rejection tests | Covered | - |
| S4-006 | 4.4.1-4.4.4 | Dotted module identities, aliases, default visibility (every item exported; no `(export ...)` form), qualified lookup, provider-qualified imported macros, macro-bearing body local shadowing, and macro import ordering. | Implemented | `tests/integration/implicit_core_prelude.tl`, `tests/integration/macro_module_local_shadow*.tl`, `tests/safety/imported_module_macro_bare_name_reject.tl`, `tests/inline/module_macro_generated_stdlib_vector_qualified_*`, public-tool LSP/import fixtures, `src/compiler_load.tl` dotted/package import smokes, `src/compiler_typecheck.tl` macro import smokes | Covered | - |
| S4-007 | 4.4.5 | TypeLisp linker symbol identity uses canonical module identity and declaration identity; extern/runtime symbols stay exact. | Implemented in current backend symbol model | `src/compiler_lower.tl` package-owned symbol tests, `src/tests/compiler_backend_smoke.tl`, `src/compiler_backend_tests.tl` extern metadata tests, `src/compile_manifest.txt`, C ABI fixtures | Covered | - |
| S4-008 | 4.4.6 | `include-str` embeds UTF-8 text with import-like path resolution and diagnostics. | Implemented | `src/compiler_parse_core.tl` include-str parser tests, `src/compiler_load.tl` include-str relative/stdlib-root/missing/raw-text loader tests, `src/compiler_embedded_stdlib.tl` | Covered | - |
| S4-009 | 4.4.7 | `include-bin` embeds exact binary bytes as compiler-private `(__tl_dyn-array u8)` storage with import-like path resolution and diagnostics; compiler-owned `include-str-lzss` emits a deterministic compressed runtime-static payload from the file's exact bytes, binary included. The internal embedded-TLCI deployment path adds a separate versioned canonical-Huffman envelope without changing ordinary/package `.tlci` bytes. | Implemented | parser and loader smokes, `tests/integration/include_bin.tl`, `src/compiler_lzss_tests.tl` malformed-envelope tests, `src/compiler_embedded_stdlib_tlci_codec_tests.tl`, `scripts/verify-embedded-stdlib-payload.sh`, `scripts/verify-embedded-stdlib-tlci.sh` (deterministic exact envelope expansion) | Covered | - |
| S4-009A | 4.4.8 | `include-str-comptime` exposes exact text only to macro CTFE, and `comptime.expr-binary-data` emits opaque static byte payloads. | Implemented | parser and loader smokes, `tests/integration/include_str_comptime.tl` | Covered | - |
| S4-010 | 4.4.9 | Top-level and expression `cfg` predicates support names, `all`, `any`, `not`, target OS predicates, and inactive-branch parsing rules. | Implemented | `src/compiler_parse_core.tl` cfg parser tests, `src/compiler_load.tl` cfg import tests, `scripts/check-stage1-wrapper.sh`, `scripts/check-codegen-target-parity.sh` | Covered | - |
| S4-011 | 4.5 | Inline `(test ...)` items are ignored by production commands and owned by `typelisp test` harness generation. | Implemented | `scripts/verify-inline-tests.sh`, `tests/inline/*.tl`, `stdlib/tests/*`, `src/test_cli_core.tl` | Covered | - |
| S4-012 | 4.6 package | `typelisp.pkg` manifests, kind/entry defaults, local and git dependencies, lockfile replay/update, package cache, DAG builds, and package output paths. | Implemented | `scripts/check-stage1-wrapper.sh`, `scripts/verify-public-tools.sh`, `src/compiler_load.tl` package manifest/import tests, `src/build_cli_core.tl` tests, `src/tests/package_lock_smoke.tl`, `src/tests/package_cache_smoke.tl` | Covered | - |
| S4-013 | 4.6 defenum/defstruct | Top-level `defenum` and `defstruct` behavior delegates to the type-system rules. | Implemented | Enum/struct integration fixtures and parser/typechecker smokes | Covered | - |
| S4-014 | 4.6.1 | Cleanup-owning struct and enum declarations, field/payload cleanup metadata, generated cleanup functions, active-variant LIFO cleanup, and escape/copy restrictions. | Implemented | `src/compiler_parse_core.tl` cleanup metadata tests, `src/compiler_typecheck.tl` cleanup aggregate/resource/move tests, `src/compiler_lower.tl` cleanup aggregate shape tests, `tests/integration/vector_cleanup_consuming_iterator.tl` | Covered | - |
| S4-015 | 4.7.2 | Context-aware structural Copyable/MoveOnly classification for arrays, tuples, structs, enums, lifetimes, cleanup metadata, and cycles; the current fixed-array transition permits non-Copy elements, compatibility reads/literals/stores, consuming literal-index path moves, exact-path reinitialization, checked take-with-init, move-safe caller-supplied replacement, and owned/borrowed fixed-array patterns; permanent non-Copy global places with borrow-only access; non-consuming inspection; bounded path-sensitive scalar-loop move joins; and diagnostics for non-copyable values. | Implemented transition behavior, public-place assignment, and general move-safe replacement; other uniform structural moves remain staged | `src/compiler_typecheck.tl` direct move-kind, `compiler-typecheck-move-owner-array-ref-*`, and end-to-end structural/poison/owner/macro fixtures; `tests/integration/fixed_array_take.tl`, `tests/integration/replace_places.tl`, `tests/integration/replace_large_values.tl`, `tests/integration/fixed_array_match_pattern.tl`, `tests/integration/clone_deep_copy.tl`, `tests/integration/array_push_aggregate.tl`, `tests/integration/tuple_match_pattern.tl`, `tests/integration/unified_place_syntax.tl`, `tests/integration/composed_place_projection.tl`, `tests/safety/fixed_array_take_*`, `tests/safety/replace_*`, `tests/safety/fixed_array_match_pattern_*_reject.tl`, `tests/safety/tuple_*_reject.tl`, `tests/safety/loop_move_*`, `tests/safety/auto_borrow_*_reject.tl`, `tests/safety/global_noncopy_access_ok.tl`, and `tests/safety/global_move_*_reject.tl` | Partial | #6215 |
| S4-016 | 4.6.3 | Recursive inline aggregate cycles require explicit indirection; finite-layout analysis rejects unboxed cycles. | Implemented finite-layout analysis | `src/compiler_typecheck.tl` recursive aggregate layout fixtures, boxed list/tree acceptance tests, inline-cycle diagnostics | Covered | - |

## Section 5: Expressions

| ID | SPEC section | Normative behavior | Implementation status | Coverage owner/test(s) | Gap status | Follow-up |
| --- | --- | --- | --- | --- | --- | --- |
| S5-001 | 5.1 | Literal forms produce their specified types. | Implemented | `tests/integration/arithmetic.tl`, `tests/integration/f32_scalar.tl`, `tests/integration/print_char.tl`, `tests/integration/string_length.tl`, SPEC doctests | Covered | - |
| S5-002 | 5.2 | Lexical scoping and variable lookup order prefer locals, parameters, then globals. | Implemented | `tests/integration/control_flow.tl`, `tests/integration/functions.tl`, parser/typechecker smokes | Covered | - |
| S5-003 | 5.3 | Direct/indirect calls evaluate arguments left-to-right, pass register and stack args, and handle function pointer values. | Implemented | `tests/integration/call_eval_order.tl`, `tests/integration/function_pointer_values.tl`, `tests/integration/many_args.tl`, `tests/integration/tail_call_stack_args.tl`, C ABI fixtures | Covered | - |
| S5-004 | 5.4 | Integer arithmetic wraps where specified; division/remainder and invalid shifts trap deterministically; floats support accepted arithmetic. | Implemented | `tests/integration/arithmetic.tl`, `tests/integration/div_zero_trap.tl`, `tests/integration/min_div_neg1_trap.tl`, `tests/integration/shl_*_trap.tl`, `tests/safety/*trap.tl`, `tests/spmd/{masked_if,byte,word}_shift_{negative,large}_trap.tl`, `tests/integration/f32_scalar.tl` | Covered | - |
| S5-005 | 5.5 | Comparisons support integer and float types; string equality uses string helpers. | Implemented | `tests/integration/arithmetic.tl`, `tests/integration/f32_scalar.tl`, `tests/integration/string_eq.tl` | Covered | - |
| S5-006 | 5.6 | `if`, bracket-arm `cond`, `when`, and `unless` enforce boolean tests and branch/body typing; flat `cond` is rejected. | Implemented | `tests/integration/control_flow.tl`, `tests/integration/cond.tl`, `stdlib/tests/core_macros_*.tl` | Covered | - |
| S5-007 | 5.7-5.11 | `let`, `begin`, scalar `while`, zero-operand scalar `break`/`continue`, normalized-place `set!`, move-safe `replace!`, and `ann` sequencing, typing, mutation, ownership, unit/value behavior, nearest-loop targeting, lambda boundaries, and scoped cleanup/arena unwinding. | Implemented | `tests/integration/control_flow.tl`, `tests/integration/scalar_while_break_continue.tl`, `tests/integration/struct_field_set.tl`, `tests/integration/unified_place_syntax.tl`, `tests/integration/composed_place_projection.tl`, `tests/integration/replace_places.tl`, `tests/integration/replace_large_values.tl`, `tests/safety/replace_*`, `tests/safety/while_*_reject.tl`, `tests/safety/struct_field_set_*_reject.tl`, `tests/safety/deref_*_reject.tl` | Covered | - |
| S5-008 | 5.12 | Expression `cast` delegates to the section 3.8 scalar matrix. | Implemented | `tests/integration/overflow_casts.tl`, `tests/integration/u64_float_casts.tl`, `tests/safety/unsupported_cast_reject.tl` | Covered | - |
| S5-009 | 5.12.1 | Explicit and contextual `init` build valid ZII values or reject unsupported/ambiguous cases. | Implemented for current eligible surface | `tests/integration/make_array_init.tl`, `tests/integration/make_array_zero.tl`, `tests/integration/fixed_array.tl`, `src/compiler_parse_core.tl`, `src/compiler_typecheck.tl` positive and rejection self-tests | Covered | - |
| S5-010 | 5.13 | `match` supports enum, borrowed enum, struct, owned/borrowed tuple and fixed-array destructuring, scalar, string literal, wildcard, exhaustive arms, payload/field bindings, and arm type merging. | Implemented for current surface | `tests/integration/enum_match.tl`, `tests/integration/string_match.tl`, `tests/integration/struct_match_pattern.tl`, `tests/integration/tuple_match_pattern.tl`, `tests/integration/fixed_array_match_pattern.tl`, `tests/integration/register_group_borrowed_match.tl`, `tests/integration/lifetime_enum_borrow_return.tl`, parser/typechecker smokes | Covered | - |
| S5-011 | 5.14 | Lambdas cover non-capturing lowering, structurally Copy and move-only environments, raw-pointer/reference/aggregate captures, fixed-array captures, and shared/mutable/consuming concrete call capabilities. | Implemented; captured-name assignment and required-fact erasure are rejected by design | `tests/integration/lambda_*.tl`, `tests/integration/tail_indirect_closure.tl`, `tests/safety/lambda_mut_capture_*`, `tests/safety/lambda_fnonce_*`, `tests/safety/lambda_noncopy_capture_*`, `src/tests/compiler_typecheck_smoke.tl` lambda positive/rejection self-tests | Covered | - |
| S5-012 | 5.15 | `foreach` scalar semantics, contiguous map/zip lowering (including checked multi-destination straight-line maps), scalar and AVX2/AVX-512 gather-only generated-Vec backing reads for i32/i64/f32/f64 (including checked gather reductions), native borrowed Slice sources/destinations with explicit data/length lowering, offset and empty/sub/full/multi-gang/tail ranges over generated-Vec and fixed-array backing, tail masks, supported lane types, uniform/varying rules, early-exit rejection, public SIMD vector/mask/varying source type deferral, and SPMD alias/per-lane-reference-escape/scatter safety rejections. | Integration coverage uses generated vector/native-slice borrowed-storage surfaces; lower-level fixtures and benchmarks retain private buffers where intentionally scoped | `tests/integration/spmd_foreach.tl`, `tests/integration/spmd_gather_read.tl`, `benchmarks/ispc/perfbench_gathers/`, `scripts/verify-ispc-perfbench-gathers.sh`, `tests/spmd/*.tl`, `tests/spmd/multi_output_i64.tl`, `tests/spmd/store_alias_i64.tl`, `tests/spmd/multi_output_bounds_trap.tl`, `tests/spmd/native_slice_surface_i64.tl`, `scripts/verify-spmd-simd.sh`, `tests/safety/spmd_slice_alias_call_reject.tl`, `tests/safety/spmd_slice_lane_mut_ref_escape_reject.tl`, `tests/safety/spmd_slice_scatter_reject.tl`, `tests/safety/spmd_foreach_return_reject.tl`, `tests/safety/spmd_foreach_try_reject.tl`, `tests/safety/spmd_foreach_break_reject.tl`, `tests/safety/spmd_foreach_continue_reject.tl`, `tests/safety/spmd_*_reject.tl`, `tests/safety/spmd_public_vector_type_reject.tl`, `tests/safety/spmd_public_mask_type_reject.tl`, `tests/safety/spmd_varying_type_reject.tl` | Covered | - |
| S5-013 | 5.15 | Public `(program-index)` and `(program-count)` lane identity forms. | Implemented for `foreach` maps and `spmd-reduce` value expressions | `tests/integration/spmd_lane_identity_scalar.tl`, `tests/spmd/lane_identity_i64.tl`, `tests/spmd/lane_identity_reduce_i64.tl`, `scripts/verify-spmd-lane-identity.sh`, `tests/safety/spmd_program_*_reject.tl` | Covered | - |
| S5-014 | 5.15 | Masked varying `if`, scalar-lane and enum varying `match`, varying `while`, and unsupported branch restrictions. | Implemented for the current scalar reference and AVX2/AVX-512 subsets: unit-result branches, nested branch-mask composition, loop-carried varying-while masks, AVX2/AVX-512 scalar-lane literal/wildcard/catch-all varying matches, AVX2/AVX-512 enum tag/payload varying matches with active-lane-only scalar payload loads, value-producing scalar lane selects, integer `bit-and`/`bit-or`/`bit-xor` across all contiguous lane widths, guarded shifts across all integer lane widths with active-lane-only traps, i16/u16 array writes, bool mask selects, and explicit diagnostics for deferred forms | `tests/safety/spmd_masked_if_*_reject.tl`, `tests/safety/spmd_varying_match_*_reject.tl`, `tests/spmd/masked_if_*.tl`, `tests/spmd/{masked_if,byte,word}_shift_*.tl`, `tests/spmd/varying_while_i64.tl`, `tests/spmd/varying_while_nested_i64.tl`, `tests/spmd/masked_if_varying_while_i64.tl`, `tests/spmd/varying_match_i64.tl`, `tests/spmd/varying_match_enum_payload.tl`, `scripts/verify-spmd-simd.sh`, SPEC check examples `spmd-masked-if-*` | Covered | - |
| S5-015 | 5.15 | `spmd-reduce`, `spmd-scan`, `spmd-broadcast`, and `spmd-shuffle` operators, supported types, empty range semantics, purity/body rules, native Slice reduce/scan operands, scalar scan/shuffle lowering, SIMD reduction/broadcast vectorization, AVX2/AVX-512 canonical contiguous range-wide scan prefixes with cross-gang carry and scalar tails, and native AVX2/AVX-512 numeric shuffle permutations with ordered validation and gang-correct tails. | Implemented for current slice | `tests/integration/spmd_reduce_scalar.tl`, `tests/integration/spmd_scan_scalar.tl`, `tests/spmd/native_slice_surface_i64.tl`, `tests/integration/spmd_scan_*_trap.tl`, `tests/integration/spmd_shuffle_{scalar,simd,types,reduce,tail_selector}.tl`, `tests/integration/spmd_shuffle_*trap.tl`, `benchmarks/spmd_{reduce,scan,shuffle}/`, `tests/safety/spmd_reduce_f32_min_reject.tl`, `tests/safety/spmd_reduce_f64_min_reject.tl`, `tests/safety/spmd_scan_*_reject.tl`, `tests/safety/spmd_shuffle_*_reject.tl`, `scripts/verify-spmd-simd.sh` | Covered | - |
| S5-016 | 5.15 | Runtime SIMD dispatch `defdispatch` validates variants and selects cached best supported backend. | Implemented | `tests/spmd/runtime_dispatch_select.tl`, `scripts/verify-spmd-runtime-dispatch.sh`, `stdlib/tests/cpu_api.tl` | Covered | - |
| S5-017 | 5.15 | V1 same-program private out-of-line SPMD call ABI specialization, hidden active-mask passing, inactive-lane safety, scalar/AVX2/AVX-512 implementation targets, the separate scalar/AVX-512 imported-package catalog ABI, supported scalar lane argument/result types, and named deferrals for indirect/function-value, extern, dispatch, recursive, and aggregate/string/array/function call shapes. | Same-program frontend analysis, IR lowering, and native private-call emission are implemented in all three modes. AVX2 uses caller-owned 256-bit stack-block values, AVX-512 uses private high vector/opmask registers with cycle-safe mask staging, and scalar uses the ordinary internal ABI. Imported `:spmd-callable` helpers retain their specified scalar/AVX-512 package ABI; shapes outside v1 retain focused diagnostics. | `src/tests/compiler_spmd_call_lower_{scalar,avx512}_smoke.tl`, `tests/spmd/private_helper_*.tl`, `scripts/verify-spmd-simd.sh`, `scripts/verify-spmd-package-calls.sh`, `tests/safety/spmd_varying_call_reject.tl`, `tests/safety/spmd_masked_if_call_reject.tl`, direct inline-helper fixtures in `tests/spmd/inline_helper_*.tl`, and `tests/spmd/README.md` call coverage notes | Covered | - |
| S5-018 | 5.16 | `with-arena` scopes allocation, rejects escapes, supports nested regions, and lowers to mark/reset on Linux and Windows. | Implemented | `tests/integration/with_arena_*.tl`, `tests/integration/arena_escape_stress.tl`, `tests/safety/region_escape_reject.tl` | Covered | - |
| S5-019 | 5.16, 7.3 | `with-escape` clones supported results out of a first-class scratch arena and rewinds the scratch arena. | Implemented for current cloneable surface | `tests/integration/with_escape_clone_out.tl`, `tests/safety/with_escape_raw_i64_reject.tl`, `stdlib/tests/arena_patterns.tl`, `src/compiler_typecheck.tl` | Covered | - |
| S5-020 | 5.17 | Comptime reflection primitives, `TypeInfo.Slice`, stable `type-kind`/opaque `type-key` Slice composition, Slice `array-element-type`, nominal metadata, array/function/struct/enum indexing, canonical module-value signature lookup, reference referent extraction, and runtime rejection. | Implemented selfhost v1 | `tests/integration/comptime_scalar.tl`, `tests/integration/comptime_module_value_reflection.tl` (local/imported/stdlib/generated module views), `tests/integration/slice_generated_decl.tl`, `src/tests/compiler_typecheck_reflection_smoke.tl`, `src/tests/compiler_ctfe_smoke.tl` (Slice CTFE/type-key diagnostics, ref/mut-ref/lifetime preservation, lazy index states), `src/tests/compiler_tlci_native_producer_smoke.tl`, `tests/bootstrap_comptime_short_variants_probe.tl`, `tests/safety/runtime_type_literal_reject.tl` | Covered | - |
| S5-021 | 5.17.1 | `.tlci` container/header/hash/metadata helpers validate and round-trip images; package builds emit host comptime images; `typelisp inspect` renders valid images and reports parser diagnostics. | Metadata-only macro-free package emission and deterministic multi-entry code-bearing package images are implemented. The byte-16 v2 host-platform enum distinguishes Linux SysV and Windows x64 without changing the header; production emitters stamp the build host independently of the consumer runtime target, metadata stays portable, and legacy/unknown/opposite-host code fails before allocation or calls. Package collection filters to package-owned declarations, sorts fully qualified canonical identities, and passes explicit source captures to the shared native producer. Kind-8/schema-1 source-set provenance hashes the exact deterministic package transcript, is shared by metadata-only and code-bearing images, and exposes fail-closed missing/legacy, unsupported, malformed, unreadable, and mismatch admission results; inspector output renders the binding. Supported bodies -- general quasiquote templates (nested calls, literals, plain symbols, unquoted operands, unquote-splicing), array intrinsic nodes, match-on-type-kind computed bodies, list-length computed ifs, identity/builder plain bodies, and computed folds -- execute through direct named host callbacks and commit the same AST shapes as interpreted CTFE; let-rooted bodies whose binding inits are computed expressions evaluate through host session locals and emit compiled template, bracket-clause, borrow, array-set, match, and let declarations the same way; shapes outside that subset retain registered no-result fallback shells. Dynamically unquoted `set!` places reconstruct variable, struct-field, and box assignments through a value-consuming callback with interpreted diagnostic parity; all 103 checked-in embedded stdlib entries are fully native, and the coverage ratchet pins shell, partial, and interpreted-arm counts to zero. A source-derived checked manifest now pins every identity's arity, variadic shape, result kind, assigned semantic witness, expected status/fuel, and comparison policy; the required two-host 15-fixture differential observes every identity in that witness through native/default, byte-identical explicit-root, and forced-source routes with exact assembly and authored-diagnostic parity. Malformed request/handle, native abort, and fuel controls retain no partial committed result. The object-image emitter copies phi inputs into their destinations on every incoming edge, so merges whose phi destination differs from its inputs read the staged value. Container/callback ABI v2 adds a validated name-to-GOT-slot import section and a static host registry; the loader resolves every import before W^X and reports package-qualified missing-name diagnostics. Metadata schema v2 includes validated stable-order SPMD-callable records. The immutable v2 corpus pins layouts, platform tags, malformed diagnostics, inspector output, and emitter compatibility on both targets; legacy images intentionally inspect as ambiguous and missing-or-legacy for source admission. Published Linux and Windows compilers route trusted embedded-stdlib macros through the production catalog by default. Native `Expr`, `Module`, and `Decls` commits, zero-fallback coverage, forced-source assembly/diagnostic parity, five-row reset/remap stress, and embedded bootstrap fixpoint are required gates. The same-commit mutation bootstrap additionally pins the interpreted-producer to native-successor handoff for one package-qualified transformer and requires exact assembly, image, envelope, source-hash, producer-identity, and provenance convergence. General dependency discovery now source-admits job-owned catalogs; cross-platform package gates prove metadata-only images remain zero-entry/no-map catalogs and code-bearing dependency macros use the native route with forced-source parity. The code-bearing gate also mutates a producer after its image is built, proves stale admission retains zero mapped bytes and falls back to source, rebuilds to changed native behavior with identical assembly, invalidates the old same-process capability, admits the replacement key, and restores byte-identical output. The focused stale/rebuild and metadata-only gates remain permanent required evidence. Trusted dependency images hydrate relocatable AST, checked facts, and SPMD metadata into consumer-owned storage as an exact-identity, all-or-source transaction. The required diamond differential maps one canonical catalog per package and pins 9 hits, 8 native dispatches, 4 direct `Expr`, 1 `Module`, 2 `Decls`, one shell learn/cache hit, package-qualified defining identities, repeated imports/generated keys, ordered splicing, zero-map/9-fallback forced source, authored/fuel diagnostic attribution, malformed-fragment rollback, byte-identical assembly, and equal runtime behavior. | `tests/tlci/corpus/`, `tests/tlci/package_dependency_transition.tl`, `scripts/verify-tlci-corpus.sh`, `scripts/verify-compile-profile.sh`, `scripts/verify-stdlib-tlci-identity-differential.sh`, `scripts/verify-tlci-native-route-stress.sh`, `scripts/verify-package-metadata-tlci.sh`, `scripts/verify-package-native-tlci.sh`, `scripts/verify-package-surface-tlci.sh`, `scripts/check-bootstrap-fixpoint.sh`, `scripts/verify-bootstrap-fixpoint-control.sh`, `tools/embedded-stdlib-tlci/identity-fixtures.tsv`, `tools/embedded-stdlib-tlci/bootstrap-mutation-witness.tl`, `src/package_source_core.tl`, `src/tlci_core.tl`, `src/compiler_dependency_tlci.tl`, `src/compiler_comptime_diagnostics_core.tl`, `src/compiler_tlci_native_producer.tl`, `src/tests/compiler_tlci_native_producer_smoke.tl`, `src/tests/comptime_host_smoke.tl`, cross-platform `src/tests/tlci_loader_smoke.tl`, `src/inspect_cli_core.tl`, `src/tlci_loader.tl`, backend ABI bridge tests, `scripts/check-stage1-wrapper.sh`, `scripts/verify-public-tools.sh`, `scripts/verify-selfhost-cli-build-run.sh` | Covered | - |
| S5-022 | 5.18 | Compile-time `size-of`, `align-of`, and `offset-of` layout queries reject invalid use and produce stable metadata. | Implemented | `tests/integration/repr_c_f32_fields.tl`, `tests/integration/nested_repr_c_aggregate.tl`, `src/tests/compiler_typecheck_reflection_smoke.tl`, typechecker layout diagnostics | Covered | - |
| S5-023 | 5.19 | Resource `with` initializes left-to-right, cleans LIFO, prevents resource escape, and composes with future recoverable propagation. | Implemented for current resource cleanup surface | `tests/integration/resource_with_order.tl`, `tests/safety/sync_mutex_guard_*_reject.tl`, stdlib IO/process tests, selfhost typechecker/lowerer smokes | Covered | - |
| S5-024 | 5.20 | `unsafe` expression blocks permit only explicitly unsafe raw-pointer/syscall/manual-arena operations while preserving ordinary typechecking. | Implemented | `tests/safety/safe_raw_pointer_read_reject.tl`, `tests/safety/safe_syscall_reject.tl`, `tests/safety/unsafe_syscall_defined.tl`, `tests/safety/manual_arena_reset_unsafe_reject.tl`, `stdlib/ffi.tl` inline tests, `stdlib/arena.tl` inline tests | Covered | - |

## Section 6: Built-in Functions and Runtime

| ID | SPEC section | Normative behavior | Implementation status | Coverage owner/test(s) | Gap status | Follow-up |
| --- | --- | --- | --- | --- | --- | --- |
| S6-001 | 6.1 | Import-free fixed-array construction/length/read/place-write forms, compiler-private dynamic-buffer intrinsics, move-safe `replace!`, borrowed Slice length/read/write/subviews, string helpers, concat, literal formatting with implicit/indexed/named/captured selection, byte width/fill/alignment/sign/zero options, literal/dollar/star precision counts, complete fixed-width primitive Display including fixed float precision/half-even rounding, and canonical owner-module nominal display, panic/error, and alias signatures behave as specified and trap on bounds, negative dynamic width/precision, invalid Slice ranges, or invalid lengths; runtime-count public `make-array`, public `array-push!`, and `array-data` are rejected, while `__tl_array-data` retains unsafe low-level owner-only semantics. | Implemented for the fixed-array public and private-buffer surfaces; compiler-owned array/Slice/replace operations and stdlib-owned string/format wrappers cover their documented surfaces | `src/compiler_parse_core.tl` fixed-construction/public-removal/private-buffer parser checks, `tests/integration/fixed_array_core_ops.tl`, `tests/safety/fixed_make_array_*_reject.tl`, `src/compiler_backend_tests.tl` private-intrinsic target emission, `tests/integration/private_array_data.tl`, `tests/safety/private_array_data_safe_reject.tl`, `tests/integration/replace_places.tl`, `tests/integration/replace_large_values.tl`, `stdlib/tests/format_api.tl`, `stdlib/tests/format_primitive_display.tl`, `stdlib/tests/format_float_precision.tl`, `stdlib/tests/format_*_reject.tl`, `tests/inline/float_exponent_frontend.tl`, `tests/integration/io_format_print.tl`, `tests/integration/tlci_native_format_scanner.tl`, `tests/integration/format_owner_*`, `tests/integration/string_*.tl`, `tests/integration/slice_whole_array.tl`, `tests/integration/slice_views.tl`, `tests/integration/make_array_*_trap.tl`, `tests/integration/stdlib_substring_*_trap.tl`, `tests/safety/replace_*`, `tests/safety/slice_*`, safety corpus, and raw-pointer/FFI private-buffer fixtures | Covered | - |
| S6-002 | 6.2 | Backend-owned runtime is limited to the allocator/arena/TLS substrate, ABI/entry shims, `tl_memcpy`/`tl_memchr`, the tlci raw entry bridge, profile accessors tied to allocator counters, and currently tracked migration targets; Linux/Windows page paths and TLS/current-arena policy are target-owned. | Implemented, including `tl_arena_make_atomic`, atomic current-arena allocation paths, the `tl_tlci_call_image_entry` bridge, and checked runtime helper ownership/global-symbol inventories | `tests/integration/tl_alloc.tl`, `tests/integration/tl_alloc_huge_trap.tl`, `tests/integration/region_*.tl`, `tests/integration/thread_runtime.tl`, `stdlib/tests/arena_atomic_api.tl`, native link scripts, `src/compiler_backend_tests.tl` runtime inventory/page-call tests | Covered | - |
| S6-003 | 6.3 | Operator aliases expand to their canonical builtin/helper forms. | Implemented | `tests/integration/string_eq.tl`, `tests/integration/substring.tl`, `tests/integration/print_string.tl`, stdlib tests | Covered | - |
| S6-004 | 6.4 | `stdlib/io.tl` file handle API covers opaque handles, open modes, close/double-close errors, streaming read/write/flush, and platform unsupported results. | Implemented for current stdlib surface | `stdlib/io.tl` inline tests, `tests/integration/stdlib_io.tl`, `tests/integration/read_file_missing_trap.tl`, `tests/integration/write_file_invalid_trap.tl` | Covered | - |
| S6-005 | 6.5 | Landed safe task-threading subset: raw substrate, structural transfer/share classifier with named atomic-owner proof for checked spawn captures and channel sends, non-consuming bounded completion observation, safe spawn/join for `i64`/`bool`/`unit` and the concrete `String` / owned generated `(vector i64)` / `(Box i64)` atomic-arena join wrappers, generated `(channel i64)` scalar module, concrete `ChannelI64PairChannel`, concrete `ChannelString`, generated close-safe `(mutex i64)` lexical guards, and atomic arena allocation support with four-worker retained-buffer contention checking across multiple chunks. | Implemented for current public surfaces | `examples/safe_threading.tl`, `tests/integration/thread_runtime.tl`, `tests/integration/thread_safe_i64.tl`, `tests/integration/thread_safe_bool_unit.tl`, `tests/integration/thread_safe_unit.tl`, `tests/integration/thread_safe_string.tl`, `tests/integration/thread_safe_i64_vec.tl`, `tests/integration/thread_safe_box_i64.tl`, `tests/integration/thread_safe_aggregate_capture.tl`, `tests/integration/sync_channel_thread_runtime.tl`, `tests/integration/sync_channel_pair_thread_runtime.tl`, `tests/integration/sync_channel_string_thread_runtime.tl`, `tests/integration/sync_mutex_thread_runtime.tl`, `stdlib/tests/thread_api.tl`, `stdlib/tests/sync_api.tl`, `stdlib/tests/arena_atomic_api.tl`, `tests/safety/thread_spawn_*`, `tests/safety/thread_join_*`, `tests/safety/sync_channel_*`, and `tests/safety/sync_mutex_guard_*_reject.tl` | Covered | - |
| S6-006 | 6.5 | Concrete/generated aggregate task-thread transfer includes move-based aggregate channels, typed joins through spanning atomic arenas, and public docs plus the structural safety corpus. | Implemented: `ChannelI64PairChannel` moves classifier-approved aggregate messages through runtime-owned storage; `ChannelString` moves atomic-arena-owned text through the same send checker path; `ThreadString`, `ThreadI64Vec`, and `ThreadBoxI64` copy joined aggregate results into spanning atomic arenas; generated `channel`, `mutex`, and `thread.handle` module entry points expose the supported concrete families | `examples/safe_threading.tl`, `tests/integration/sync_channel_pair_thread_runtime.tl`, `tests/integration/sync_channel_string_thread_runtime.tl`, `tests/integration/thread_safe_i64_vec.tl`, `tests/integration/thread_safe_box_i64.tl`, `tests/safety/thread_spawn_*`, `tests/safety/thread_join_*`, and `tests/safety/sync_channel_*` cover aggregate channel/join behavior and structural rejection | Covered | - |

## Section 7: Memory Model

| ID | SPEC section | Normative behavior | Implementation status | Coverage owner/test(s) | Gap status | Follow-up |
| --- | --- | --- | --- | --- | --- | --- |
| S7-001 | 7.1 | Stack slots, downward stack growth, compile-time frame sizing, and 16-byte call alignment. | Implemented | `tests/integration/many_args.tl`, `tests/integration/register_scalar_homes.tl`, `src/compiler_backend_tests.tl` stack/call-shape tests, C ABI fixtures | Covered | - |
| S7-002 | 7.2 | Heap allocation goes through `tl_alloc`; no GC or general `free`; per-thread default arenas live to program/thread lifetime; fatal page-allocation failures are allocation-free, and Windows reports immediately captured `VirtualAlloc` provenance. | Implemented | `tests/integration/tl_alloc.tl`, `tests/integration/tl_alloc_huge_trap.tl`, `tests/integration/windows_allocation_abort.tl`, `tests/integration/thread_runtime.tl`, region tests, and `src/compiler_backend_tests.tl` Windows allocation-failure shape coverage | Covered | - |
| S7-003 | 7.3 | `with-arena` is the safe scoped reclamation surface; active-arena allocation policy rejects region escapes. | Implemented | `tests/integration/with_arena_*.tl`, `tests/integration/region_*.tl`, `tests/safety/region_escape_reject.tl`, `stdlib/string.tl` inline tests, `stdlib/text_buf.tl` inline tests, `stdlib/tests/arena_policy_escape_*.tl` | Covered | - |
| S7-004 | 7.3 | Lifetime owner classes and lexical outlives model constrain references, regions, first-class arenas, `in-arena`, and atomic arena owner classes. | Implemented for lexical references/regions, ordinary first-class arena helpers, atomic arena creation, safe dynamic allocation-target surfaces, and cookbook double-buffer/unload patterns | Reference/arena safety corpus, `examples/arena_lifetimes.tl`, `src/tests/compiler_typecheck_smoke.tl`, `tests/integration/in_arena.tl`, `tests/safety/in_arena_inner_region_escape.tl`, `tests/safety/in_arena_raw_i64_reject.tl`, `tests/safety/arena_cookbook_*`, `stdlib/arena.tl` inline tests, `stdlib/tests/arena_atomic_api.tl`, `stdlib/string.tl` inline tests, `stdlib/text_buf.tl` inline tests, `stdlib/tests/arena_patterns.tl`, `stdlib/tests/arena_policy_escape_*.tl` | Covered | - |
| S7-005 | 7.3 | `with-escape`, safe phase reset/destroy, and manual arena helpers distinguish safe clone-out/proven invalidation from unsafe reset/destroy. | Implemented | `examples/arena_lifetimes.tl`, `tests/integration/with_escape_clone_out.tl`, `tests/integration/arena_phase_rewind_destroy.tl`, `tests/safety/with_escape_raw_i64_reject.tl`, `tests/safety/arena_mark_handle_mismatch_reject.tl`, `tests/safety/arena_phase_*`, `tests/safety/arena_destroy_*`, `tests/safety/arena_cookbook_*`, `stdlib/arena.tl` inline tests, `stdlib/tests/arena_patterns.tl`, `tests/safety/manual_arena_reset_unsafe_reject.tl` | Covered | - |
| S7-006 | 7.3 | Atomic arena allocation target and safe cross-thread owner policy for values allocated in spanning owners. | Implemented: the atomic allocation target is consumed by concrete aggregate join/channel surfaces, recognized by the shared transfer classifier for named atomic `in-arena` results, and safely reset/destroyed after joined-user release | `examples/safe_threading.tl`, `examples/arena_lifetimes.tl`, `stdlib/tests/arena_atomic_api.tl`, `tests/integration/thread_safe_string.tl`, `tests/integration/thread_safe_i64_vec.tl`, `tests/integration/thread_safe_box_i64.tl`, `tests/integration/arena_atomic_rewind_destroy_safe.tl`, `tests/safety/thread_spawn_i64_atomic_string_capture_ok.tl`, `tests/safety/arena_atomic_*`, `tests/safety/arena_cookbook_channel_unload_reject.tl`, and `src/compiler_backend_tests.tl` cover allocation, runtime, owner classification, and invalidation | Covered | - |
| S7-007 | 7.4 | Raw pointers are nullable/copyable address values; unsafe memory operations carry caller obligations; aggregate `ptr-addr-of` has a pinned v1 place contract. | Implemented for scalar and aggregate local/parameter storage, struct-field paths, fixed-array element paths, and coherent reads after writes through register-resident aggregate aliases | Raw-pointer safety fixtures, `stdlib/ffi.tl` inline tests, `tests/integration/ptr_addr_of_places.tl`, `tests/integration/raw_pointer_register_addr_of.tl`, both native manifests, and compiler typecheck/lower raw-pointer tests cover the surface | Covered | - |
| S7-008 | 7.5 | Globals live in data/rodata, string literal bytes are rodata, and `String` points at inline ptr/len storage. | Implemented | `tests/integration/global_initializer.tl`, `tests/integration/aggregate_globals.tl`, `tests/golden/*.s`, `src/compiler_backend_tests.tl` global/string data-shape tests | Covered | - |
| S7-009 | 7.6 | Aggregate handles use structural Copyable/MoveOnly source classification; storage reached through a memory-class by-value parameter is read-only, non-consuming inspection is compatibility borrow-like behavior, mutation requires owned/mutable access, returns may heap-promote, and `clone` deep-copies supported shapes. | Structural classification and current checker/lowering implemented | `src/compiler_typecheck.tl` structural copy/poison/owner/macro move fixtures, `tests/integration/clone_deep_copy.tl`, `tests/integration/array_push_aggregate.tl`, `tests/integration/by_value_memory_aggregate_explicit_mut.tl`, `tests/integration/register_resident_*`, `tests/safety/struct_field_set_*_reject.tl`, `src/compiler_typecheck.tl` move-checker and parameter-provenance fixtures | Covered | - |

## Section 8: Backend Capabilities and Limitations

| ID | SPEC section | Normative behavior | Implementation status | Coverage owner/test(s) | Gap status | Follow-up |
| --- | --- | --- | --- | --- | --- | --- |
| S8-001 | 8.1 | Supported backend surface includes scalar arithmetic, floats, bool/char/unit, control flow, calls, locals/globals, casts, enums, structs, arrays, strings, stdlib I/O/FFI, imports, and native Linux/Windows targets. | Implemented | Integration manifests, stdlib tests, native link scripts, backend smoke tests, public-tool build/run coverage | Covered | - |
| S8-002 | 8.3 | Implemented limitations table entries stay covered as they move from unsupported to supported: tuple ABI, fixed-array returns, aggregate globals, raw pointers, move checking, `with`, SPMD, defdispatch, package manager, REPL, LSP. | Implemented where marked implemented | Section-specific rows in this matrix; integration/native manifests; safety corpus; public-tool scripts, including semantic signature-help transcripts; backend smoke tests | Covered | - |
| S8-003 | 8.3 | Mutable lambda captured-name assignment is formally rejected. | Rejected by design | `src/compiler_typecheck.tl` captured assignment negative and shadowing-positive cases | Covered | - |
| S8-004 | 8.3 | Direct/self tail jumps and supported indirect function-value tail jumps are optimized; unsupported ABI shapes conservatively demote to ordinary calls. | Implemented for supported shapes with conservative demotion fallback | `tests/integration/tail_*.tl`, native manifests, `src/compiler_backend_tests.tl` direct/indirect tail-call emission checks | Covered | - |
| S8-005 | 8.2-8.3 | The per-backend SPMD matrix distinguishes supported native execution, supported scalar-reference execution, deliberate language exclusions, and implementation gaps tracked by open issues. | Scalar, AVX2, and AVX-512 support is reconciled for varying control flow, gather-only reads, explicit integer atomic scatter, reductions/scans, shuffle, broadcast, lane identities, same-program private calls, dispatch, and modulo `i8`/`u8` multiplication. | `scripts/verify-spmd-simd.sh`, `scripts/verify-spmd-runtime-dispatch.sh`, `scripts/verify-spmd-broadcast.sh`, `scripts/verify-spmd-lane-identity.sh`, `tests/spmd/byte_mul_value_types.tl`, `tests/safety/spmd_*_reject.tl`, and SPMD rows S5-012 through S5-017 | Full | #6684 |
| S8-006 | 8.1 | Source-authored semantic errors retain usable source paths and spans through parsing, imports, normalization, typechecking, lowering, macro specialization, CLI rendering, and LSP conversion; origin-free internal/synthetic rebuild errors use the documented 1:1 fallback. | Implemented | `src/compiler_parse_core.tl` pins cold origin runs, tails, adjacent runs, and reset; `src/compiler_typecheck.tl` covers extern/dispatch/lifetime/cleanup declaration errors plus nested, wrapped, normalized, and imported paths; safety, lower, smoke, compiler-diagnostic, and public-tool LSP coverage pins expression, member, primary, and secondary span behavior | Covered | - |

## Section 9: Error Handling

| ID | SPEC section | Normative behavior | Implementation status | Coverage owner/test(s) | Gap status | Follow-up |
| --- | --- | --- | --- | --- | --- | --- |
| S9-001 | 9 | `panic` and `error` print to stderr, exit 134, have `never`, and merge into branch/match result types. | Implemented | `tests/integration/read_file_missing_trap.tl`, `tests/integration/write_file_invalid_trap.tl`, `tests/integration/stdlib_substring_*_trap.tl`, SPEC compile examples through doctest/public tools | Covered | - |
| S9-002 | 9 | Function-local `(return expr)` has bottom type, is valid only inside functions/lambdas, runs active `with` cleanup and `with-arena` resets, and is rejected inside `foreach`/SPMD bodies. | Implemented for current selfhost return surface | `tests/integration/early_return.tl`, `tests/integration/resource_with_order.tl`, `tests/integration/with_arena_loop.tl`, `tests/integration/with_arena_mark_stress.tl`, `tests/safety/spmd_foreach_return_reject.tl`, `src/compiler_typecheck.tl`, `src/compiler_lower.tl` cleanup/arena return-shape smokes | Covered | - |
| S9-003 | 9 | Recoverable error and option families are concrete enums generated by stdlib module macros, not generic traits/type erasure. | Implemented for stdlib option/result modules and hand-written monomorphic families | `stdlib/result.tl`, `tests/integration/maybe_result.tl`, `tests/integration/generated_option_result_families.tl`, SPEC compile examples | Covered | - |
| S9-004 | 9 | `(try expr)` propagates compatible Result-like and Option-like families, rejects incompatible or malformed families, and is rejected inside `foreach`/SPMD bodies. | Implemented for convention-compatible concrete Result-like and Option-like enums | `src/compiler_typecheck.tl` try positive/negative smokes, `src/compiler_lower.tl` Result/Option lowering shape tests, `tests/integration/register_group_try.tl`, `tests/integration/generated_option_result_families.tl`, `tests/safety/result_macro_try_error_mismatch_reject.tl`, `tests/safety/spmd_foreach_try_reject.tl` | Covered | - |
| S9-005 | 9 | Scalar `(break)` and `(continue)` have bottom type, are valid only inside scalar `while`, target the nearest loop, run active `with` cleanup and `with-arena` resets for scopes they leave, do not cross lambda/function boundaries, carry no values, and are rejected inside `foreach`/SPMD bodies. | Implemented | `tests/integration/scalar_while_break_continue.tl`, `tests/safety/while_*_reject.tl`, `tests/safety/spmd_foreach_break_reject.tl`, `tests/safety/spmd_foreach_continue_reject.tl` | Covered | - |

## Section 10: CLI

| ID | SPEC section | Normative behavior | Implementation status | Coverage owner/test(s) | Gap status | Follow-up |
| --- | --- | --- | --- | --- | --- | --- |
| S10-001 | 10 | Root command list and common options expose `build`, `check`, `compile`, `doc`, `fmt`, `init`, `inspect`, `lint`, `lsp`, `new`, `repl`, `run`, and `test`. | Implemented | `tests/public-tools/cli-command-surface.txt`, `scripts/verify-public-tools.sh`, selfhost command wrappers in compile manifest | Covered | - |
| S10-002 | 10 | `compile`, `build`, `run`, package build, opt-level/profile/lock flags, stdlib roots, cfg, target/backend mode, batch, emit-ir, textual IR dumps, pass tracing, and on-demand IR verification behave as command-specific help says. Profile-enabled batch compilation exposes ordinal teardown telemetry without changing normal output. | Implemented | `scripts/verify-public-tools.sh`, `scripts/verify-ir-observability.sh`, `scripts/verify-selfhost-cli-build-run.sh`, `scripts/check-stage1-wrapper.sh`, `scripts/verify-compile-profile.sh`, `scripts/measure-compile-rss.sh`, `scripts/measure-compile-batch-memory.ps1`, `tests/golden/optimizer_fold.after-fold.ir`, package smoke tests, `scripts/check-codegen-target-parity.sh` | Covered | - |
| S10-003 | 10 | `fmt`, default and staged `lint` rules (including the audited-module `raw-arena-op` boundary and opt-in category-specific `--name-case` declaration/binder checks), package-kind dead-code roots, `test`, doctest/doc generation, and default package discovery use public CLI semantics. | Implemented | `scripts/check-tl-format.sh`, `scripts/check-tl-lint.sh`, `src/tests/lint_driver_smoke.tl`, `scripts/verify-public-tools.sh`, `scripts/verify-inline-tests.sh`, `scripts/verify-doc-tests.sh`, `scripts/verify-doc-site.sh` | Covered | - |
| S10-004 | 10 | `repl` supports `.help`, `.type`, `.exit`, remembers declarations, evaluates expressions through scratch build/run, and recovers from errors. | Implemented | `tests/public-tools/repl/*`, `tests/public-tools/selfhost-repl/*`, `scripts/verify-public-tools.sh` | Covered | - |
| S10-005 | 10 | Linux build/run uses `as` and `ld`; Windows build/run uses Clang/lld-link and CRT-linked runtime helpers. | Implemented | `scripts/verify-native-link-linux.sh`, `scripts/verify-native-link-windows.sh`, `scripts/ci-verify.sh`, native integration manifests | Covered | - |

## Section 11: ABI Reference

| ID | SPEC section | Normative behavior | Implementation status | Coverage owner/test(s) | Gap status | Follow-up |
| --- | --- | --- | --- | --- | --- | --- |
| S11-001 | 11.1 | System V AMD64 uses independent integer/float argument register sequences, stack args, return registers, callee-saved registers, and 16-byte call alignment; the internal TypeLisp borrowed-Slice pair uses first `%rdi`/`%rsi` and returns `%rax`/`%rdx`. | Implemented | `tests/integration/c_abi_sysv_register_aggregate_args.tl`, `tests/integration/c_abi_sysv_tag_only_enum.tl`, `tests/integration/c_abi_sysv_two_register_return.tl`, `tests/integration/c_abi_f32_scalar.tl`, `tests/integration/slice_whole_array.tl`, emitted-source checks in `src/compiler_backend_tests.tl`, `tests/integration/many_args.tl`, backend smoke tests | Covered | - |
| S11-002 | 11.2 | Windows x64 uses shared four-register slots, shadow space, return registers, and CRT-owned startup with `main`; the internal TypeLisp borrowed-Slice pair uses first `%rcx`/`%rdx` and returns `%rax`/`%rdx`. | Implemented | `tests/integration/c_abi_win64_*.tl`, `tests/integration/many_args.tl`, `tests/integration/slice_whole_array.tl`, emitted-source checks in `src/compiler_backend_tests.tl`, `scripts/verify-native-link-windows.sh`, backend smoke tests | Covered | - |
| S11-003 | 11.3 | Data layout uses stable primitive sizes/alignment, pointer-sized handles, declaration-order structs, tagged-union enums, ordinary 8/8 references, and borrowed Slice references at 16/8 (pointer then signed length); `(:repr c)` remains compatibility metadata only. | Implemented | `tests/integration/repr_c_f32_fields.tl`, `tests/integration/nested_repr_c_aggregate.tl`, `tests/integration/array_stride_24byte.tl`, `tests/integration/ptr_offset_inline_aggregate_stride.tl`, Slice register-group/classifier assertions in `src/compiler_backend_tests.tl`, `src/compiler_typecheck.tl` layout-query diagnostics, and backend layout smoke tests | Covered | - |
