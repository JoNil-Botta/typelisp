(import tests.proof_reuse_safety_provider as provider)
(import (provider.proof-reuse-fixed-constructor-argument i64) as fixed_i64)
(import (provider.proof-reuse-fixed-constructor-argument u8) as fixed_u8)

(define (main) : i64
  42)