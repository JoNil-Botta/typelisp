(import tests.proof_reuse_safety_provider as provider)
(import (provider.proof-reuse-fixed-field-access i64) as fixed_i64)
(import (provider.proof-reuse-fixed-field-access u8) as fixed_u8)

(define (main) : i64
  42)